# Copiez en .env et adaptez. Sans clé, tout tourne en mode "mock".

# ── Environnement ──────────────────────────────────────────────────────────
# À FIXER SUR RAILWAY : ENV=production.
#
# Toute valeur autre que dev / development / local / test est traitée comme de la
# production : /docs, /redoc et /openapi.json sont fermés, et /health se limite à
# un mot d'état par dépendance. Le schéma OpenAPI publiait autrement la liste des
# routes d'administration — dont purge-offers et reset-price-history — avec leurs
# paramètres et le nom de l'en-tête attendu. Ces routes exigent bien
# ADMIN_SYNC_TOKEN, mais il n'y a aucune raison d'en publier le plan.
ENV=dev
DEBUG=true

# Infrastructure (laisser vide pour démarrer sans dépendances)
DATABASE_URL=postgresql+asyncpg://filon:filon@postgres:5432/filon
REDIS_URL=redis://redis:6379/0
QDRANT_URL=http://qdrant:6333

# Routage LLM par tâche : mock | deepseek | kimi | glm
LLM_PROVIDER_DEFAULT=mock
LLM_PROVIDER_REASONING=mock
LLM_PROVIDER_LONG=mock

# Clés (renseignez pour activer le fournisseur correspondant)
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat

KIMI_API_KEY=
KIMI_MODEL=moonshot-v1-128k

GLM_API_KEY=
GLM_MODEL=glm-4

# Données produits réelles (Google Shopping via SerpApi)
# Sans clé : mode "LLM estimé". Avec clé : vrais produits (photo, prix, lien).
SERPAPI_API_KEY=
SERPAPI_GL=be
SERPAPI_HL=fr

# ── Affiliation & catalogue Awin ────────────────────────────────────────────
# AWIN_PUBLISHER_ID n'est PAS secret (il figure dans les liens). Le token API et
# la clé de feed SONT des secrets : à définir sur Railway, jamais dans le code.
AWIN_PUBLISHER_ID=3005443
# Token API Publisher (liste des marchands inscrits + liens affiliés)
AWIN_API_TOKEN=
# Clé de feed produits (Toolbox → Create-a-Feed dans l'UI Awin). Peut différer
# du token API. Sans elle, l'ingestion des offres produits est désactivée.
AWIN_FEED_API_KEY=
# Régions du catalogue (codes pays, séparés par des virgules)
AWIN_REGIONS=BE,FR,LU,NL
# Garde-fou : nb max de feeds ingérés par run (0 = tous)
AWIN_FEED_LIMIT=0
# Garde-fou mémoire : nb max de lignes ingérées par feed (0 = illimité)
AWIN_MAX_ROWS_PER_FEED=0
# Cron interne : rafraîchit le catalogue toutes les N heures (0 = désactivé).
# Mettre 6 pour tout automatiser (prix + historique) sans service Railway en plus.
AWIN_AUTO_SYNC_HOURS=0

# Jeton protégeant les endpoints d'admin (POST /api/catalog/sync/*). Secret.
# Sans valeur, ces endpoints répondent 403 : c'est le comportement souhaité par
# défaut. Ne le renseigner que pour déclencher un sync ou une reconstruction.
ADMIN_SYNC_TOKEN=

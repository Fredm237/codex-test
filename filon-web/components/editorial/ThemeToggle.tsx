"use client";

/**
 * L’identité FILON est désormais volontairement claire et chaude. Le composant
 * reste exporté pour préserver les emplacements de navigation existants, mais
 * ne rend plus de commande permettant de revenir à un thème sombre.
 */
export function ThemeToggle(_props: { compact?: boolean }) {
  return null;
}
